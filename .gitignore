# The four download scripts used to hardcode an OpenNeuro API token and were excluded
# so it could not enter history. The token was replaced with an environment read on
# 2026-08-07, so they are tracked now. It never reached a commit — and it should still be
# rotated, since it sat in cleartext.

__pycache__/
*.pyc
.DS_Store
